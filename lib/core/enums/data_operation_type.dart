enum DataOperationType {
  insert,
  update,
  delete,
  view,
}

enum FileType {
  excel,
  csv,
  pdf,
}

enum SnackbarType {
  success,
  error,
  info,
  warning,
}

enum TransferType {
  higherBand,
  lowerBand,
  sameBand,
}

enum GradeGap {
  gap,
  noGap,
}
